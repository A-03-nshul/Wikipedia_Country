services:
  - type: web
    name: wikipedia-outline-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Assignment4:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.16 